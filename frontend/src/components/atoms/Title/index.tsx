import React from 'react';
import TitleDiv from './styles';

const Title: React.FC = () => {
    return (
        <TitleDiv>
            <h1>Vendas</h1>
        </TitleDiv>
    );
};

export default Title;
